# Loan-default-prediction
To run this project firstly run the model.py 
then type the command in terminal as streamlit run app.py
the project will open 
then select sample1.csv file to see the result
